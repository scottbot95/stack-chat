import initialState from './initialState';
import {} from './constants';
import {} from './actions';

export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
